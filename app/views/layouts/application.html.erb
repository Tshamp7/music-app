<!DOCTYPE html>
<html>
  <head>
    <title>MusicApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
         <nav class="navbar navbar-expand-lg navbar-light bg-light">
         <div class="collapse navbar-collapse" id="navbarNav">
            <a class="navbar-brand" href="#">Musiq</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
            
              <div class="container">
                <ul class="navbar-nav">
                  <li class="nav-item active">
                    <a class="nav-link" href=" <%= page_path("home")%> ">Home <span class="sr-only">(current)</span></a>
                  </li>
                  <li class="nav-item">
                    <a class="nav-link" href=" <%= page_path("about") %> ">About</a>
                  </li>
                  <li class="nav-item">
                    <a class="nav-link" href="<%= new_session_path %>">Login</a>
                  </li>
                   <li class="nav-item">
                    <a class="nav-link" href="<%= new_user_path %>">Signup</a>
                  </li>
                </ul>
              </div>  
            </div>

            <div class="d-flex justify-content-end align-items-center">
            <div class="collapse navbar-collapse" id="navbarNav">
              <ul class="navbar-nav">
                <% if !current_user.nil? %>
                <li class="nav-link disabled">Logged in as:</li> 
                <%= link_to "#{current_user.email}", user_path(current_user), class: "nav-link" %>
                
                <li class="d-flex nav-item align-items-center">
                  <form action="<%= session_url %>" method="POST">
                    <input type="hidden"
                          name="authenticity_token"
                          value="<%= form_authenticity_token %>">
                    <input type="hidden" value="delete" name="_method" />
                    <input class="btn btn-secondary btn-sm" type="submit" value="Logout" />
                  </form>
                </li>
                <% end %>
              </ul>
            </div>
            </div>
          </nav>
        
    <%= yield %>
  </body>
</html>
