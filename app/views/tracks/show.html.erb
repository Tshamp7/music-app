<h3>  Track info for <%= @track.title %></h3>
<p> Album: <%= @album.title %> </p>
<p> Track Number: <%= @track.track_number %> </p>
<% unless @track.lyrics.nil? %>
  <%= @track.lyrics %>
<% end %>
<br>
<%= button_to "Back to Album", album_path(@album), method: :get %>
<%= button_to "Delete", track_path(@track), method: :delete, data: { confirm: "Are you sure you want to delete this?" } %>
<%= button_to "Edit Track", track_path(@track), method: :get %>