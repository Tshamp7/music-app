<br>
<div class="container">
    <div class="card">
        <div class="card-header">
            <h2>Edit <%= @track.title %> </h2>
        </div>
        <div class="card-body">
            <%= render 'form' %>
            <br>
            <%= button_to "Back to Track", track_path(@track), method: :get, class: "btn btn-sm btn-outline-secondary"%>
        </div>
    </div>
</div>