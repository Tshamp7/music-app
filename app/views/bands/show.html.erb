<h2>Welcome to the page for <%= @band.name %></h2>

<h3> Albums </h3>
<ul>
    <% @band.albums.each do |album| %>
        <li><%= link_to "#{album.title}", album_path(album) %></li>
    <% end %>
</ul>

<%= button_to "Return to all Bands", bands_path, method: :get %>
<br>
<%= button_to "Add an Album", new_band_album_path(@band), method: :get %>